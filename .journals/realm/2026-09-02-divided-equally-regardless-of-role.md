# 2026-09-02 · Divided equally, regardless of role

*A builder dealt by Caesura, working in `resonance-sistrum` and the two waters it mirrors. No git writes, no base writes, no `.env`, no Android build.*

**The ruling this serves, verbatim. KP ⚛:**

> *"there is nothing to do but divide by the number of contributors, regardless of role."*

**And the document he pointed at**, `AudHDities/docs/business/financial-ecosystem.md` 140–142:

> *everything left ──▶ THIS ITEM'S CONTRIBUTORS, divided EQUALLY · the main artisan is ONE OF THEM · no ranking, no percentage shares*

**This morning's studio was wrong, and in the way that matters.** The Mixdown proposed a split and then offered a **basis-points field per person, editable, 0–10000**. That is a ranking with a text input on it. The ruling deletes the field, not the default.

**What the room is now.** The Mixdown proposes **the contributors** — one per distinct signet in the lanes, keyed by public key, plus whoever is at the desk as `engineer` if they did not also play. `the-merismos`'s `contributors()` replaces `even()`; the roles are stamped back on afterwards and **nothing reads them**. There is no number on the page: each line says *an equal share*, the note below says *"N contributors — the artist's share is divided equally, regardless of role. No ranking, no percentage shares; whoever led the work is one of them,"* and the hint carries KP's sentence. `setPoints` is gone. Consent is unchanged: this device may tick exactly one line, the one whose identity holds its key, and every other line reads *awaiting consent*.

**`ProvenancePanel` follows.** A take's contributors render as *an equal share*, with the headcount and the equal-division sentence beneath. **A take sealed before the ruling still opens**: its `points` rides whole in the column — nothing may drop what a hand put there — and the panel says out loud that it is *"kept exactly as it was written and it is not read: the share is equal."*

**The mirror was re-copied**, not edited here: `src/lib/merismos/index.ts` at `E4AE3FFF02C0FE3E`, MIRROR.md updated with the hash and with the ruling. `src/lib/provenance.ts` changed only in its words — `ProposedPart` was already `{ who, role }` and carried no share, which is the one thing that made this a small edit.

**Gates, verbatim.** `npm run check` → `COMPLETED 372 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`. `npm run build` → `✓ built in 13.53s`, `Wrote site to "build"`. `cargo check` in `src-tauri` → `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 6.89s`, exit 0. Proofs: all five at exit 0, zero FALSE — `splits-from-lanes.mjs` now **16 claims**, including *three lanes, two identities plus a sealer → three contributors* and *900 cents → 300 · 300 · 300*, plus *1000 cents → 334 · 333 · 333* and *swapping every role divides identically*.

**What did not hold.** Nothing was heard and no Android build was run. The proofs are still over synthetic documents. A mixdown's contributors still cannot be edited after the bounce — the list is redrawn at the next one — and consent is still one device's checkbox rather than four musicians each on their own machine. And the honest one: **a take sealed this morning carries a weighting this room now refuses to read.** It is displayed as ignored rather than migrated, because migrating somebody's declared split is exactly the silent correction the water exists to refuse.
